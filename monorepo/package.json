{
  "name": "monorepo",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "preview": "turbo run preview",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "dev:user": "concurrently -c \"cyan,magenta,yellow\" \"pnpm --filter shell dev\" \"pnpm --filter user build --watch\" \"pnpm --filter user preview\"",
    "dev:dashboard": "concurrently -c \"cyan,magenta,yellow\" \"pnpm --filter shell dev\" \"pnpm --filter dashboard build --watch\" \"pnpm --filter dashboard preview\"",
    "dev:task": "concurrently -c \"cyan,magenta,yellow\" \"pnpm --filter shell dev\" \"pnpm --filter task build --watch\" \"pnpm --filter task preview\"",
    "dev:management": "concurrently -c \"cyan,magenta,yellow\" \"pnpm --filter shell dev\" \"pnpm --filter management build --watch\" \"pnpm --filter management preview\"",
    "dev:setting": "concurrently -c \"cyan,magenta,yellow\" \"pnpm --filter shell dev\" \"pnpm --filter setting build --watch\" \"pnpm --filter setting preview\""
  },
  "devDependencies": {
    "@turbo/gen": "^2.9.18",
    "concurrently": "^10.0.3",
    "prettier": "^3.7.4",
    "turbo": "^2.9.18",
    "typescript": "5.9.2"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
